[project]
name = "aiohttp-webrtc"
version = "0.1.0"
description = "Aiohttp + aiortc WebRTC audio/video receiving tutorial"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.12.0",
    "aiortc>=1.9.0",
]
